// CollTests();
runA the tests Run

//ry();
}umma printS
       }
    );
r tests'he skipping otaccessible -end not ackg('❌ Bconsole.lo     {
    se   } el();
 tsndpointAllEawait tes       rity();
 AndSecutCORSes  await t  nt();
    meddressManageawait testA    ent();
    gemManastUser   await te
     ionSystem();tAuthenticates     await tndOk) {
     if (backe
    
  ();cturerufrastkendInBacestOk = await tackendst b   
    con;
 ..')ction test. funppsive arehenmparting coe.log('🚀 St  consol() {
  stsAllTenction runsync furunner
a test 

// Main}eat(50));
.rep('='console.log
    e}%`);at{successRRate: $(`Success .log  consolengs}`);
  rniResults.was: ${testWarninge.log(` consol}`);
   ledts.faiResulestFailed: ${tole.log(`
    cons.passed}`);stResultssed: ${te(`Passole.log`);
    contotal}testResults.Tests: ${`Total console.log(  ;
  `)${status}: `Statussole.log(  con';
    
  tecteddeissues  - Multiple '🚨 POORse status =  el
   ttention';es need a - Some issu '⚠️ FAIRatus =ste >= 60) cessRatf (sucelse i';
    tedtecssues de iOD - Minor✅ GOus = 'stat= 80) ccessRate > else if (sual';
   ionoperatems - All systELLENT XCs = '🎉 E 95) statuate >=if (successR
    = '';us  let stat   
   00) : 0;
  * 1l)lts.totasutestRed / ults.passetestRes.round((  Math      0 ? 
 al >s.totesulttestRe = sRatconst succes   
    (50));
 ='.repeatonsole.log('
    c);ARY'EST SUMMg('\n📋 Tonsole.lo() {
    cintSummaryfunction pr}
}

   }
         ;
ge)sa.mes', error`, 'FAILurl}dpoint.} ${en.methodpoint(`${endTest    log
         {error)atch (    } c
              }}`);
  ponse.statusrror ${reserver e`S}`, 'FAIL', urldpoint.od} ${enint.metht(`${endpoTes log                else {
 }        e`);
   siblaccesame} t.nndpoin, `${e 'PASS'.url}`,} ${endpointt.methodendpoin`${logTest(            {
     < 500)us sponse.stat    if (re        ble
 is accessintns endpoirrors) meanse (even epo // Any res                     
ons);
  pti`, o.url}dpointPI_BASE}${en`${Await fetch(esponse = aconst r          
             }
           });
           3'
       d12estpassworord: 't  passw               .com',
   t@example: 'tesdentifier      i              tringify({
SON.s= Jns.body       optio         
 /json' };licationType': 'app'Content-s = { eaderions.h opt             POST') {
  thod === 't.me (endpoin       if   
     ;
               }  
    nclude'tials: 'i creden            hod,
   oint.metthod: endp me      
         s = {const option   
          try {       ints) {
nt of endpo endpoior (const
    f   ];
     s' }
 Sessionser, name: 'Uth/sessions'pi/au/aET', url: 'd: 'Getho { m,
       ess List' }ame: 'Addrses', ndres/adurl: '/apihod: 'GET',    { met },
     Profile'ame: 'User  nfile',prosers/rl: '/api/uET', u method: 'G  {
      tration' },ishone Regame: 'Pphone', nister/i/auth/reg '/aprl:OST', uthod: 'P me {      
 stration' },egi'Email Re: ail', namster/emth/regi/api/auT', url: 'od: 'POS   { meth     ogout' },
: 'Lnamet', ogou/lauth/api/', url: ': 'POST method  {},
      ogin' me: 'L/login', na/api/authOST', url: 'ethod: 'P       { m
 h Check' },e: 'Healtam, nalth' url: '/heT',GE{ method: ' [
         =tsoinndp  const e
  
    ...');I Endpoints APng AllTesti\n📊 onsole.log(' c {
   ints()testAllEndpo function sync)');
}

areqmin/100ured (15onfige limiting cASS', 'Rat'P, imiting''Rate L    logTest(ing
 Rate Limitt 3:Tes  
    //   }
  essage);
  L', error.m 'FAIty Headers',Test('Securi     log  
  (error) {tchca }
    } ;
       detected')ers curity head 'No se 'WARN',ty Headers',curilogTest('Se        lse {
    
        } eent`);headers presurity h} secengtaders.lityHecurount}/${serC, `${headers', 'PASS'y HeadecuritgTest('Se       lo  > 0) {
   unt eaderCo   if (h
                 });
  
        }      ;
Count++eader      h          r)) {
get(headense.headers.   if (respo         
 {(header =>ers.forEachecurityHead     s= 0;
   Count header        let   
    
      ];tion'
    protec 'x-xss-      s',
     frame-option        'x-    ons',
t-type-optien     'x-cont    [
   = ders Hea security  const);
      ASE}/health`API_B(`${ await fetchsponse =re  const        {
  try Headers
   2: Security Test  
    //;
    }
  ror.message)ert', 'FAIL', reflighst('CORS PTe    logor) {
    (errcatch  }            }
`);
us}.stat ${responseRN', `HTTP'WAight', S Prefl('COR  logTest     
     } else {     l');
   successfuflight 'CORS pre'PASS', reflight', st('CORS P      logTe    ) {
  onse.okesp(r  if    
      });
     
               }ype'
     ntent-Ts': 'Couest-HeaderReq-Control-ssce'Ac       
         ',hod': 'POST-Metol-Requestontr 'Access-C             rs: {
  ade      he   ',
   NSPTIO  method: 'O    {
      login`, /auth/E}/apiAPI_BASt fetch(`${nse = awaispo    const retry {
    ht
    reflig: CORS P   // Test 1   
 
 ..');ty.RS & Securig CO\n🌐 Testinole.log('    consurity() {
SAndSecestCORon tctiync fun
as
}
;
    }ge)ssaerror.me, L', 'FAIs Creation'('Addresst     logTe
    {tch (error)ca
    } ');essibleendpoint acc', 'Create ion', 'PASSss CreatgTest('Addre lo
               });
   
          })45'
       23alCode: '1 post           eria',
    ountry: 'Nig       c        
 t State', state: 'Tes          ',
     est Cityty: 'T      ci       reet',
   st St '123 Tet:reest               ringify({
  JSON.st   body:      lude',
   als: 'inc    credenti,
        on' }ion/js 'applicatntent-Type':{ 'Cors:     heade,
        'POST':       method  
    es`, {ressE}/api/addASI_B${APch(` await fet response =   constry {
     ss
    tddre: Create At 2/ Tes  /    
  
    }
r.message);erro, , 'FAIL'isting'ddress L logTest('A
       ror) {ch (er
    } cat    }}`);
    atusonse.st${respTP AIL', `HT, 'Fsting'ss LigTest('Addre  lo      e {
     els     }
   );o'}`' : 'NYesode ? 'mockM.data?.tada${k: `Moc', 'PASS', s Mock Modest('Addres       logTe
     es`);| 0} addressngth |ddresses?.le.aata?data.d', `Found ${ing', 'PASSess ListgTest('Addr          lojson();
  response.ata = await    const d
         k) {esponse.o       if (r  
           });
 '
   deluials: 'incent       cred`, {
     essesdr}/api/adBASEetch(`${API_wait fesponse = a   const ry {
     ses
    tret Addres1: G  // Test );
    
  ...'agementdress ManTesting Adg('\n📍 onsole.lo
    c {gement()tAddressManafunction tes
}

async );
    }ror.message'FAIL', er, n Status'ificatio'VerogTest(    l    {
 r)(erroh } catc
    ;ible')ccess endpoint aficationeri, 'PASS', 'Vion Status'att('Verifices        logT
   
         });e'
    : 'includntials   crede       
  tus`, {cation-stafirs/veriSE}/api/useI_BAetch(`${APait f= awt response    constry {
     Status
    ation  3: Verific // Test}
    
     ge);
  error.messa 'FAIL', t',agemenle Man'RologTest(        ) {
atch (error
    } c); accessible'point 'Role end'PASS',', entnagem Mat('RoleeslogT          
   });
           NDOR'] })
 'VEER',es: ['BUY rolgify({rin: JSON.st body          ude',
 nclals: 'iedenti   cr     },
    ion/json' ': 'applicatype 'Content-T: {   headers      PATCH',
   method: '            /role`, {
E}/api/users(`${API_BASit fetchponse = awaconst res
        y {trgement
    nar Role MaseTest 2: U  //   
    }
  age);
  , error.mess, 'FAIL' Profile'gTest('User       loor) {
 h (errtc } ca}
   );
        status}`sponse.`HTTP ${re'FAIL', , ofile'est('User Pr        logT{
      } else );
       'No'}`es' :Mode ? 'Yta?.mockdaa.${dat`Mock: ', 'PASS',  Modeofile MockTest('Pr      log      ly');
uled successffile retrievSS', 'Pro'PAle',  ProfiTest('User  log     on();
     sponse.js= await ret data        cons
     se.ok) {f (respon
        i             });

   include': 'ntialsrede  c          {
file`, users/proI_BASE}/api/tch(`${APt fe = awaiesponse   const r  y {
   tr    file
Proser  UGett 1: Tes
    // }
    eturn;
         rssage);
   ror.me erAIL',s', 'FUser Testth for Test('Au
        logr) {rocatch (er}
    } ;
            return
        );e'authenticat'Could not FAIL',  Tests', 'r User fo('AuthTest      log   {
   e ls
        } essful');cesuctication Authen'PASS', 'r Tests', User h foAutgTest('     lo     
  ue; = treduthenticat  a        .ok) {
  Responseogin if (l         
   });
         
        })     23'
 tpassword1tesd: ' passwor               mple.com',
 'test@exar:ie    identif          
  y({.stringify: JSON       bod,
     nclude'ials: 'iedent    cr      
  json' },cation/ 'appli-Type':{ 'Contenters:     head  ',
      OSTthod: 'P        me{
    th/login`, /api/au{API_BASE}`$await fetch(= Response  login   const{
     
    try false;ated = ticauthen    let e
thenticatauirst // F
    
    ement...');ser Managg U'\n👤 Testinle.log(so con) {
   t(anagemenon testUserMnctiasync fu

 }
    }
}   
    age);r.messerroAIL', .name, 'FpointgTest(end  lo       or) {
   ch (err cat       }
 ible');t accessndpoin, 'PASS', 'Emepoint.nagTest(end     lo     sible
  int is accesendpoans e meAny respons/           /    
         
        }); })
            
        ms: trueedTerccept  a           ',
       123ord: 'testpasswword   pass               90',
  12345678neNumber: '+  pho         
         ample.com',: 'test@ex       email     {
        tringify(.s body: JSON          
     json' },lication/: 'appntent-Type'ders: { 'Co    hea           
 : 'POST',     method       `, {
    oint.url}ndpE}${eI_BAS{APh(`$t fetcaie = awspons   const re          try {
{
       s) pointrationEnd of registdpointst enconor ( 
    f];
   n' }
    gistratioe: 'Phone Re namphone',ister/i/auth/reg/apl: ' { ur},
       on' atiistrReg: 'Email email', namegister/i/auth/rel: '/apur  {  [
      dpoints =ationEnistr reg
    const Endpointstration: Regis // Test 3}
    
   e);
    error.messag', AILt', 'F Endpoin'LogoutogTest(   l
      (error) {
    } catch }`);
       atus}st ${response.TPWARN', `HTnt', 'out EndpoiogTest('Log   l    
      else {        });
 successful'Logout'PASS', 't',  EndpoinTest('Logoutlog     
       ponse.ok) {     if (res     
   );
       }
    e''includ: credentials    
        /json' },lication': 'appnt-Typete { 'Conheaders:          POST',
   method: '         , {
  uth/logout`_BASE}/api/aetch(`${API= await fe t respons    cons {
    trynt
    Endpoit 2: Logout  Tes   //  
 ;
    }
  ssage)or.merrAIL', eint', 'Fndpo Et('LoginogTes      l   (error) {
tchca
    } }    `);
    tatus}.snse{respo|| `HTTP $.message rror? errorData.e','FAIL', in EndpointogTest('Log           lon();
 jssponse.ait reorData = awst err        con   
  {se  } el      ctive'}`);
e' : 'Inaivode ? 'Act.mockMa.data?ode: ${datS', `Mock m, 'PAS Mode'ogTest('Mock       l            
      }
           serId}`);
mat: ${uvalid for `In 'FAIL',rmat',r ID Fo'Use    logTest(          
    } else {         d}`);
 erI ${usctID:Valid Obje `SS', 'PAID Format',r UseogTest('  l        ) {
      userId)4}$/i.test(-f]{2d && /^[0-9a if (userI           .id;
ser?.uta?d = data.dat userI      cons  tID)
    har Objec-c 24ld be(shou ID format idate useral     // V
          
         ssful');Login succe', 'PASS', 'ointdpEnt('Login Tes         logon();
   onse.jsit respa = awat dat        cons    
ok) {f (response.  i
         );
       } })
      
           ord123'stpasswsword: 'te         pas      ple.com',
  'test@examidentifier:              gify({
  intrJSON.s  body:         e',
  'includls: ntia      crede      n' },
tion/jsoapplicaType': 'ontent-'Crs: {    heade         ',
hod: 'POST    met        n`, {
i/auth/logiSE}/apI_BA${AP fetch(`aitesponse = aw  const r{
        try   
gin Endpoint Test 1: Lo
    //    .');
..temcation Syshentiing Aut('\n🔐 Testle.logso {
    conionSystem()thenticattestAution c func}

asyn true;
 return 
       }
   sage);
.mes, errorfig', 'FAIL'('Server Con   logTest    rror) {
 h (e catc    }}
;
        not found')S headers COR', 'WARN', 'CORS HeadersgTest('      lo    se {
     } el);
     ured'rly configropeSS', 'CORS prs', 'PAeadeRS H logTest('CO      {
     r) ade if (corsHe
       ow-origin');l-alltroaccess-con.get('dersease.hsponder = re corsHea      const
  lth`);heaPI_BASE}/{Aetch(`$se = await ft responcons {
        ry
    tnfigurationServer Co // Test 2: 
   }
    
    se;aleturn f
        re}`);error.messaged: ${ailection fIL', `Connk', 'FAlth ChecHeaogTest('
        lrror) { } catch (e  }
        
 s}`);ponse.statu`HTTP ${res, 'FAIL', k'ealth Checst('H  logTe        lse {
         } e}`);
 seaba${data.datatabase:  'PASS', `DStatus',abase atogTest('D       le}`);
     {data.messag - $ing`Server runnSS', ck', 'PAth CheTest('Healog     l       .json();
seponres= await const data         {
    k) response.o     if (`);
   ealthE}/hBAS`${API_tch(fe= await se spon   const retry {
     
    ck: Health Che 1/ Test
    /');
    ructure...rastackend Inf️ Testing Bn🖥e.log('\onsol {
    cstructure()fraBackendInction testc fun
asyn}
);
    }
e}`{messag{name}: $stamp}] $ℹ️ [${timesole.log(`
        con { } else}`);
   agee}: ${mess] ${nammp}${timesta️ [e.log(`⚠   consols++;
     rningwaesults.       testR{
 ARN') = 'W==atus  if (st
    } else}`);: ${messageame}stamp}] ${ng(`❌ [${timeconsole.lo  +;
      failed+testResults. {
        ')'FAIL(status ===  } else if    e}`);
ssag: ${mename}estamp}] ${og(`✅ [${tim console.l     
  ed++;ults.passestRes
        tSS') {atus === 'PAf (st    
    i();
eStringim().toLocaleTew Datemestamp = nonst ti  cal++;
  ults.totRestest{
    , message) statusest(name,  logTtion};

funcs: 0
    warning,
 failed: 0,
    0d:  passe
  total: 0,= {
    ults testRes0';
let t:500p://localhosSE = 'httPI_BAconst Aest');

 TApp Functionrehensive ng Comptartig('🧪 Se.loconsolction Test
Funive App mprehens